# 文風審校規格(_review_spec.md)

你對既有的章節摘要 HTML 做**句級文風審校**,把它從「可讀」提升到「正式、精確、通順的書面中文」。

## 目標文體

- 繁體中文書面語。句子簡短、主動語態、一個句子講一件事。
- 保留 ELI5 的親切語氣與比喻(比喻框 `.analogy` 內可以生動口語,但文法必須完整)。
- 消除 DeepL 機器翻譯腔。

## 必改清單

1. 歐化長句:三層以上「的」疊加、60 字以上無停頓長句 → 拆成短句。
2. 不當被動:「被認為/被用於/導致了...被」→ 改主動,寫出誰做什麼。
3. 冗詞套語:「進行...的動作」「做出...的增加」「在...的情況下」「對於...來說」→ 精簡成動詞或直接主詞。
4. 連接詞堆疊:「然後接著之後」→ 留一個。
5. 詞彙不一致:同一英文概念全檔必須同一譯名(例如 contractile 全檔統一「收縮」,不要一處「收縮」一處「收縮性」一處「.contractile」)。
6. 「一個」「之/的」冗餘;「做/作」誤用;「的/地/得」誤用。
7. 語體混雜:正文裡的誇飾口語(「超強」「爆表」)→ 正式詞(「顯著」「大幅超出」);但 `.analogy` 比喻框內可保留生動說法。
8. 機械直譯痕跡:「使...變得更加」「給予...以...」→ 自然中文動詞。

## 不可碰

- `<style>` 區塊:逐字保留,完全不可編輯。
- SVG:結構、座標(x/y/width/height/viewBox)不可動;`<text>` 內文字可改字詞但不可加長超過原字串 +4 字(避免溢出)。
- 所有表格結構、數字、考試數據的事實內容(措辭可修,數字與結論不可變)。
- class 名稱、章節順序、錨點 id。
- 正文净字數變化控制在 ±8%:只修句,不增删段落。

## 方法(強制)

1. 先用 Read 分段讀完整份檔案。
2. 只準用 Edit 做句級替換(通常 20-80 處)。禁止用 Write 整檔重寫。
3. 逐段審校:一段一段過,不放過任何彆扭句子。
4. 收尾自檢:檔案仍結尾於 </html>;`<svg>`/`<table`/`class="qa"` 數量與開工前相同(開工前先數一次記在筆記)。
5. 回報:Edit 次數 + 每類主要修正各舉 1 例(引用原文→新文)。
